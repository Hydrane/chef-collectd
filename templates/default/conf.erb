<%
def gen(conf, i = 0)
  conf.map do |k, v|
    if v.is_a?(Hash)
      "#{' ' * i}<#{k}>\n" + gen(v, i + 2) + "#{' ' * i}</#{k.split(' ').first}>\n"
    else
      "#{' ' * i}#{k} #{v}\n"
    end
  end.join('')
end
%>
<%= gen(@conf) %>
